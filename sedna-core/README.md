# sedna-core

Canonical semantic DTOs and deterministic utilities.

## Example

```java
import io.sedna.core.CanonicalOrdering;
import io.sedna.core.SemanticGraph;

SemanticGraph graph = CanonicalOrdering.canonicalize(existingGraph);
```
