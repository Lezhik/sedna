# sedna-demo

Small Spring Boot monoliths used for reverse profile detection and multi-fixture integration tests.

| Project | Domain |
|---------|--------|
| `spring-demo` | Generic CRUD demo |
| `inventory-demo` | Product inventory API |
| `order-demo` | Order management API |

Each project is an independent Gradle root with `src/main/java`.
