# CMS Reference (SEDNA)

Hand-authored Spring Boot 3 reference project for forward/reverse equivalence (Phase 2–3).

Semantic DNA fixture: `../cms-reference-fixture.sdna`

```bash
./gradlew bootRun
```

Forward codegen target layout matches this project (`io.sedna.cms`).
